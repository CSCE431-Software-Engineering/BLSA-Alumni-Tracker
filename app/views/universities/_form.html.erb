<%= form_with(model: university, class: "contents") do |form| %>
  <% if university.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(university.errors.count, "error") %> prohibited this university from being saved:</h2>

      <ul>
        <% university.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="px-3 mb-4">
    <%= form.label :University, style: "display: block" %>
    <%= form.text_field :University %>
  </div>

  <div class="px-3 mb-4">
    <%= form.submit "Save", class: "shadow bg-gray-800 hover:bg-gray-400 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded" %>
  </div>
<% end %>